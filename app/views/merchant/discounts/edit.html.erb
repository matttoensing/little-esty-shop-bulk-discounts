<h1>Edit Discount</h1>

<%= render partial: 'form', locals: { path: "/merchants/#{@merchant.id}/discounts/#{@discount.id}", method: :patch, button_text: 'Edit Discount', percentage_value: @discount.percentage.to_i, quantity_value: @discount.quantity_threshold, name_value: @discount.name } %>
